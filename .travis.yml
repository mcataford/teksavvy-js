language: node_js
node_js:
  - "8"
cache: npm

jobs:
  include:
    - stage: test
      script: 
        - npm run test
    - stage: build
      script: npm run build
      script: npx semantic-release